                <div class="col-8 project">
                    <div class="row row-cols-3">
                        {{#each project}}                        
                            <div class="project">
                                <a href="{{externalLink}}" target="_blank">
                                <img width="100%" src="{{thumbnail}}">  </a>
                                <div class="project-link">                      
                                    <a href="{{github}}" target="_blank"> {{displayName}}
                                    <i class="fab fa-github"></i></a>
                                </div>
                            </div>
                        {{/each}}
                    </div>
                </div> 